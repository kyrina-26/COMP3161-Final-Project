import faker
import random
def insert():
    print("Inserting data into the database...")
    # Code to insert data into the database goes here
    # create the most independent tabls first and then the dependent tables
    #Independent tables: users, courses. 
    # dependent tables: everything else. (easiest to fill is student, admin and instructor tables, then teaches will be next as it is more simple than enrollment, then enrollment)
    pass
if __name__ == "__main__":
    insert()